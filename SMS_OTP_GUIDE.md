# راهنمای سیستم OTP پیامک

## گام اول: اجرای Migration و Seeder

فایل زیر را در مرورگر باز کنید:

```
https://yoursite.com/haraj/public/run-sms-setup.php
```

این فایل به ترتیب:
- جداول `sms_gateways` و `otp_codes` را می‌سازد
- ۵ درگاه پیش‌فرض را در دیتابیس ثبت می‌کند
- cache را پاک می‌کند

---

## گام دوم: نصب پکیج‌های اضافی (در صورت نیاز)

| درگاه | پکیج مورد نیاز |
|-------|----------------|
| ملی پیامک (SMS.ir) | نیازی ندارد |
| قاصدک | نیازی ندارد |
| ملی پیامک (Melipayamak) | `composer require melipayamak/php` |
| ملی پیامک پترن | `composer require melipayamak/php` |
| کاوه نگار | `composer require kavenegar/php` |

---

## گام سوم: تنظیم درگاه در پنل ادمین

به آدرس زیر بروید:

```
/admin/sms-gateways
```

1. درگاه مورد نظر را انتخاب کنید
2. اطلاعات API را وارد کنید
3. دکمه "ذخیره تنظیمات" را بزنید
4. دکمه "فعال کردن" را بزنید
5. با دکمه "ارسال تست" صحت تنظیمات را بررسی کنید

---

## اطلاعات مورد نیاز هر درگاه

### SMS.ir (ملی پیامک)
- API Key از پنل sms.ir
- Secret Key از پنل sms.ir
- شماره فرستنده (خط اختصاصی)

### Melipayamak
- نام کاربری پنل melipayamak.com
- رمز عبور پنل
- شماره فرستنده

### Melipayamak Pattern
- نام کاربری و رمز عبور
- کد پترن OTP (از پنل ملی پیامک بسازید)

### قاصدک
- API Key از پنل ghasedaksms.com
- شماره فرستنده

### کاوه نگار
- API Key از پنل kavenegar.com
- شماره/خط فرستنده

---

## نحوه کارکرد سیستم

### ورود با OTP
1. کاربر روی "ورود با کد یکبار مصرف" کلیک می‌کند → `/login/otp`
2. شماره موبایل وارد می‌کند
3. کد ۶ رقمی به شماره ارسال می‌شود (اعتبار ۵ دقیقه)
4. کد را وارد می‌کند و وارد سایت می‌شود

### ثبت‌نام با تایید OTP
1. کاربر فرم ثبت‌نام را پر می‌کند
2. روی "ارسال کد" کلیک می‌کند (AJAX)
3. کد به شماره ارسال می‌شود
4. کد را در فیلد مربوطه وارد می‌کند
5. فرم ثبت‌نام submit می‌شود و کد تایید می‌شود

### Rate Limiting
- حداکثر ۳ درخواست در ۱۰ دقیقه برای هر شماره
- کدهای قبلی با ارسال کد جدید باطل می‌شوند
