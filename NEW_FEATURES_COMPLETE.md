# پیاده‌سازی ویژگی‌های پیشرفته خرید فوری و تمدید حراجی

## خلاصه تغییرات

تمام 4 ویژگی درخواستی با موفقیت پیاده‌سازی شدند:

### ✅ ویژگی 1: غیرفعال کردن خرید فوری
**وضعیت**: کامل شده

**توضیح**: وقتی پیشنهادی بالاتر یا مساوی قیمت خرید فوری ثبت می‌شود، قابلیت خرید فوری به صورت خودکار غیرفعال می‌شود.

**فایل‌های تغییر یافته**:
- `app/Services/BidService.php`

**منطق**:
```php
// Disable buy now if bid exceeds buy now price
if ($listing->buy_now_price && $amount >= $listing->buy_now_price) {
    $listing->buy_now_price = null;
}
```

---

### ✅ ویژگی 2: ویرایش قیمت خرید فوری توسط فروشنده
**وضعیت**: کامل شده

**توضیح**: فروشنده می‌تواند حتی با وجود پیشنهاد فعال، قیمت خرید فوری را ویرایش کند (بدون نیاز به تایید ادمین). قیمت جدید باید بالاتر از بالاترین پیشنهاد باشد.

**فایل‌های تغییر یافته**:
- `app/Services/ListingService.php` - اجازه به‌روزرسانی buy_now_price
- `app/Http/Requests/UpdateListingRequest.php` - اعتبارسنجی buy_now_price
- `resources/views/listings/edit.blade.php` - نمایش فیلد buy_now_price برای آگهی‌های با پیشنهاد فعال

**ویژگی‌ها**:
- فیلد buy_now_price در صفحه ویرایش برای آگهی‌های با پیشنهاد فعال نمایش داده می‌شود
- اعتبارسنجی: قیمت باید بالاتر از بالاترین پیشنهاد باشد
- پیام راهنما: "باید بالاتر از بالاترین پیشنهاد (X تومان) باشد"
- به‌روزرسانی بدون نیاز به تایید ادمین

---

### ✅ ویژگی 3: رفع مشکل تمدید حراجی در 5 دقیقه آخر
**وضعیت**: کامل شده

**توضیح**: اگر تمدید خودکار فعال باشد و پیشنهادی در 5 دقیقه آخر ثبت شود، زمان پایان حراجی 5 دقیقه تمدید می‌شود.

**فایل‌های تغییر یافته**:
- `app/Services/BidService.php`

**منطق**:
```php
// Auto-extend auction if bid is placed in last 5 minutes
if ($listing->auto_extend) {
    $now = now();
    $endsAt = $listing->ends_at;
    $minutesRemaining = $now->diffInMinutes($endsAt, false);
    
    // If less than 5 minutes remaining, extend by 5 minutes
    if ($minutesRemaining >= 0 && $minutesRemaining < 5) {
        $listing->ends_at = $endsAt->addMinutes(5);
        
        // Log the extension
        \Log::info('Auction auto-extended', [
            'listing_id' => $listing->id,
            'old_end_time' => $endsAt->toDateTimeString(),
            'new_end_time' => $listing->ends_at->toDateTimeString(),
            'bid_amount' => $amount,
            'user_id' => $user->id,
        ]);
    }
}
```

**نکات**:
- فقط در صورت فعال بودن `auto_extend` کار می‌کند
- فقط در 5 دقیقه آخر اعمال می‌شود
- هر بار 5 دقیقه به زمان پایان اضافه می‌شود
- تمدید در log ثبت می‌شود

---

### ✅ ویژگی 4: محاسبه صحیح خرید فوری با سپرده
**وضعیت**: کامل شده

**توضیح**: اگر کاربر قبلاً در حراجی پیشنهاد داده (سپرده بلاک شده)، هنگام خرید فوری فقط اختلاف قیمت خرید فوری و سپرده را پرداخت می‌کند.

**فایل‌های تغییر یافته**:
- `app/Http/Controllers/ListingController.php` - متد `participate`

**منطق**:
1. بررسی می‌شود آیا کاربر قبلاً در حراجی شرکت کرده (رکورد `AuctionParticipation` دارد)
2. اگر بله:
   - مبلغ پرداختی = `buy_now_price - deposit_amount`
   - فقط اختلاف از موجودی کسر و بلاک می‌شود
   - سپرده قبلی همچنان بلاک باقی می‌ماند
3. اگر خیر:
   - مبلغ پرداختی = `buy_now_price`
   - کل مبلغ از موجودی کسر و بلاک می‌شود

**مثال**:
- قیمت خرید فوری: 500,000 تومان
- سپرده بلاک شده: 100,000 تومان
- مبلغ پرداختی: 400,000 تومان (اختلاف)

---

## تست‌ها

### تست خودکار
فایل تست ایجاد شده: `public/test-new-features.php`

این فایل تمام 4 ویژگی را بررسی می‌کند:
1. بررسی غیرفعال شدن خرید فوری
2. لینک به صفحه ویرایش برای تست ویرایش buy_now_price
3. بررسی وضعیت تمدید خودکار
4. محاسبه مبلغ خرید فوری با سپرده

### تست دستی

#### تست ویژگی 1 (غیرفعال کردن خرید فوری):
1. یک حراجی با قیمت خرید فوری 500,000 تومان ایجاد کنید
2. پیشنهاد 500,000 تومان یا بیشتر ثبت کنید
3. بررسی کنید که دکمه خرید فوری غیرفعال شده است

#### تست ویژگی 2 (ویرایش buy_now_price):
1. یک حراجی با پیشنهاد فعال باز کنید
2. به صفحه ویرایش بروید
3. بررسی کنید که فیلد "قیمت خرید فوری" نمایش داده می‌شود
4. قیمتی بالاتر از بالاترین پیشنهاد وارد کنید
5. ذخیره کنید و بررسی کنید که بدون نیاز به تایید ادمین به‌روزرسانی شده است

#### تست ویژگی 3 (تمدید خودکار):
1. یک حراجی با تمدید خودکار فعال ایجاد کنید که 3 دقیقه دیگر تمام می‌شود
2. پیشنهاد جدیدی ثبت کنید
3. بررسی کنید که زمان پایان 5 دقیقه جلو رفته است
4. لاگ را بررسی کنید: `storage/logs/laravel.log`

#### تست ویژگی 4 (خرید فوری با سپرده):
1. با یک کاربر در حراجی شرکت کنید (سپرده بلاک شود)
2. پیشنهاد بدهید
3. دکمه خرید فوری را بزنید
4. بررسی کنید که فقط اختلاف قیمت از موجودی کسر شده است

---

## نکات مهم

### ویژگی 2 (ویرایش buy_now_price):
- فقط فیلدهای `description`، `shipping_methods`، و `buy_now_price` قابل ویرایش هستند
- سایر فیلدها (عنوان، دسته‌بندی، قیمت شروع، زمان‌ها، تصاویر) غیرفعال می‌شوند
- اعتبارسنجی در `UpdateListingRequest` انجام می‌شود
- پیام خطا: "قیمت خرید فوری باید بالاتر از بالاترین پیشنهاد (X تومان) باشد."

### ویژگی 3 (تمدید خودکار):
- فقط در صورت فعال بودن checkbox "تمدید خودکار" کار می‌کند
- فقط در 5 دقیقه آخر اعمال می‌شود
- هر پیشنهاد جدید در 5 دقیقه آخر، 5 دقیقه به زمان پایان اضافه می‌کند
- تمدید در log ثبت می‌شود برای debugging

### ویژگی 4 (خرید فوری با سپرده):
- بررسی می‌شود که آیا کاربر رکورد `AuctionParticipation` دارد
- اگر دارد: فقط اختلاف پرداخت می‌شود
- اگر ندارد: کل مبلغ پرداخت می‌شود
- تراکنش wallet با توضیح مناسب ثبت می‌شود

---

## فایل‌های تغییر یافته

1. `app/Services/BidService.php`
   - غیرفعال کردن خرید فوری
   - تمدید خودکار در 5 دقیقه آخر

2. `app/Services/ListingService.php`
   - اجازه به‌روزرسانی buy_now_price برای آگهی‌های با پیشنهاد فعال

3. `app/Http/Requests/UpdateListingRequest.php`
   - اعتبارسنجی buy_now_price برای آگهی‌های با پیشنهاد فعال

4. `resources/views/listings/edit.blade.php`
   - نمایش فیلد buy_now_price برای آگهی‌های با پیشنهاد فعال
   - نمایش پیام راهنما
   - غیرفعال کردن سایر فیلدها

5. `app/Http/Controllers/ListingController.php`
   - محاسبه صحیح خرید فوری با سپرده

---

## دستورات لازم

```bash
# پاک کردن cache
D:\xamp8.1\php\php.exe artisan config:clear
D:\xamp8.1\php\php.exe artisan view:clear
D:\xamp8.1\php\php.exe artisan cache:clear

# تست
# مراجعه به: http://localhost/haraj/public/test-new-features.php
```

---

## تاریخ تکمیل
27 فوریه 2026

## وضعیت
✅ تمام ویژگی‌ها با موفقیت پیاده‌سازی شدند
